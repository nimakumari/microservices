# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: add-task-ingress
#   annotations:
#     kubernetes.io/ingress.class: addon-http-application-routing  
# spec:
#   rules:
#   - host: add-task.6weathers.com
#     http:
#       paths:
#       - pathType: Prefix
#         path: "/"
#         backend:
#           service:
#             name: add-task
#             port: 
#               number: 80

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-add-tasks-api-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: add-task-api.6weathers.com
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: todo-add-tasks-api
                port:
                  number: 80
